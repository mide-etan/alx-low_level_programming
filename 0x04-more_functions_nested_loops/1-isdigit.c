#include "main.h"

/**
 * _isdigit -  checks the code if it's digit
 * @c: character to be checked
 *
 *Return: return 1 if c is digit else 0
 */

int _isdigit(int c)
{
	int x = 0;

	if (c >= '0' && c <= '9')
		y = 1;

	return (y);
}
